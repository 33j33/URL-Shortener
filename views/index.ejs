<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="./style.css">
</head>

<body>
    <header>
        <h1><img src="./icon.svg">URL Shortener</h1>
        <p>A quick and easy way to shrink URL</p>
    </header>
    <main>
        <div class="form-wrapper">
            <form action="/shortenUrl" method="POST" class="form">
                <label for="fullUrlInput"></label>
                <input type="url" name="fullUrl" id="fullUrlInput" placeholder="Put in the URL to be shortened"
                    required>
                <button class="btn" type="submit">Shorten</button>
            </form>
        </div>

        <div class="table-wrappper">
            <table class="fl-table">
                <thead>
                    <th>Full Url</th>
                    <th>Shortened Url</th>
                    <th>No. of Clicks</th>
                </thead>
                <tbody>
                    <% urls.forEach((url) => { %>
                    <tr>
                        <td> <a href="<%= url.fullUrl%>"><%= url.fullUrl%></a></td>
                        <td> <a href="<%= url.shortendUrl %>"> <%= url.shortendUrl %></a></td>
                        <td> <%= url.clicks %> </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </main>

</body>

</html>